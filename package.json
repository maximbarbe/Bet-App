{
  "name": "edge-bet-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first bet tracker backed by SQLite",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
